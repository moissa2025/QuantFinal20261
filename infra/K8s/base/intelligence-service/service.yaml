apiVersion: v1
kind: Service
metadata:
  name: intelligence-service
  namespace: trading-platform
  labels:
    app: intelligence-service
spec:
  type: ClusterIP
  selector:
    app: intelligence-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

