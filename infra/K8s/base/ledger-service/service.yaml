# k8s/base/ledger-service/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: trading-platform
spec:
  selector:
    app: ledger-service
  ports:
    - port: 8080
      targetPort: 8080

