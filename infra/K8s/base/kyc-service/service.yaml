# k8s/base/kyc-service/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: trading-platform
spec:
  selector:
    app: kyc-service
  ports:
    - port: 8080
      targetPort: 8080

