apiVersion: apps/v1
kind: Deployment
metadata:
  name: reconciliation-service
  namespace: trading-platform
  labels:
    app: reconciliation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reconciliation-service
  template:
    metadata:
      labels:
        app: reconciliation-service
    spec:
      containers:
        - name: reconciliation-service
          image: reconciliation-service:v0.0.1
          imagePullPolicy: IfNotPresent

          envFrom:
            - configMapRef:
                name: platform-config

          env:
            - name: NATS_URL
              value: "nats://nats.trading-platform.svc.cluster.local:4222"
            - name: RUST_LOG
              value: "info"

          ports:
            - containerPort: 8080

