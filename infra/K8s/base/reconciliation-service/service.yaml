# k8s/base/reconciliation-service/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: reconciliation-service
  namespace: trading-platform
spec:
  selector:
    app: reconciliation-service
  ports:
    - port: 8080
      targetPort: 8080

