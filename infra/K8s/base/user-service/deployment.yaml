apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: trading-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      nodeSelector:
        pool: core-services
      containers:
        - name: user-service
          image: europe-west2-docker.pkg.dev/globalquantx-prod/user/user-service:latest
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: user-db-url
                  key: DATABASE_URL
            - name: NATS_URL
              value: nats://nats:4222
          ports:
            - containerPort: 8080

