# k8s/base/aml-monitoring-service/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: aml-monitoring-service
  namespace: trading-platform
spec:
  selector:
    app: aml-monitoring-service
  ports:
    - port: 8080
      targetPort: 8080

