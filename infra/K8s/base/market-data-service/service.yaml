apiVersion: v1
kind: Service
metadata:
  name: market-data-service
  namespace: trading-platform
spec:
  type: ClusterIP
  selector:
    app: market-data-service
  ports:
    - port: 8080
      targetPort: 8080

