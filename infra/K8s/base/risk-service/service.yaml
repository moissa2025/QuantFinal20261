apiVersion: v1
kind: Service
metadata:
  name: risk-service
  namespace: trading-platform
  labels:
    app: risk-service
spec:
  type: ClusterIP
  selector:
    app: risk-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

