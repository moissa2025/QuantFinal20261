apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: trading-platform
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: auth-service:v.0.0.1
          imagePullPolicy: IfNotPresent

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

          # Load all shared platform variables
          envFrom:
            - configMapRef:
                name: platform-config-identity

          # Explicitly inject DATABASE_URL so SQLx can connect
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: platform-config-identity
                  key: DATABASE_URL

            - name: NATS_URL
              value: "nats://nats:4222"

            - name: RUST_LOG
              value: "info"

          ports:
            - containerPort: 9001

          readinessProbe:
            httpGet:
              path: /health
              port: 9001
            initialDelaySeconds: 10
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 9001
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: trading-platform
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 9001
      targetPort: 9001

