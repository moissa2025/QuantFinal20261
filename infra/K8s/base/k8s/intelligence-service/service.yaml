apiVersion: v1
kind: Service
metadata:
  name: intelligence-service
  namespace: trading-platform
spec:
  type: ClusterIP
  selector:
    app: intelligence-service
  ports:
    - name: http
      port: 9001
      targetPort: 9001
