apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: trading-platform
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 9001
      targetPort: 9001
