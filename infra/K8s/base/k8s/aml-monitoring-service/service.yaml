apiVersion: v1
kind: Service
metadata:
  name: aml-monitoring-service
  namespace: trading-platform
spec:
  type: ClusterIP
  selector:
    app: aml-monitoring-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

