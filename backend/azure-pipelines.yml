trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.11'
  - script: |
      pip install requests psycopg2-binary nats-py
      python -m tests.runner
    displayName: 'Run backend tests'

